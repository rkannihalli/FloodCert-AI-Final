[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gobject-introspection", "pango", "cairo", "glib", "harfbuzz", "fontconfig", "freetype", "libffi", "zlib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
