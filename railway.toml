[build]
builder = "nixpacks"
buildCommand = "pip install -r artifacts/flood-cert/requirements.txt"

[deploy]
startCommand = "cd artifacts/flood-cert && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
