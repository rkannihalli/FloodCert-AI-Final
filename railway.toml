[build]
builder = "nixpacks"
buildCommand = "pip install httpx fastapi uvicorn python-multipart jinja2 weasyprint apscheduler passlib bcrypt python-jose aiofiles"

[deploy]
startCommand = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"

[nixpacks]
path = "artifacts/flood-cert"
