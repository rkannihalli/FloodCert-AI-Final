[build]
builder = "nixpacks"
buildCommand = "cd artifacts/flood-cert && pip install -r requirements.txt"

[deploy]
startCommand = "cd artifacts/flood-cert && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3
