[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt", "pip uninstall weasyprint -y || true"]

[start]
cmd = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
